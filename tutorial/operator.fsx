let a = 10
let b = 0

// comparison operators
printfn $"a = b: {a = b}"
printfn $"a <> b: {a <> b}"

// bitwise and, or
printfn $"a &&& b: {a &&& b}"
printfn $"a ||| b: {a ||| b}"

// logical and, or
let x = true
let y = false
printfn $"x && y: {x && y}"
printfn $"not x: {not x}"
